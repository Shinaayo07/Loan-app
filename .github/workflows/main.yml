name: loan-project-ci
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  workflow_dispatch:
    
  schedule:
    - cron: '0 0 * * 0' # Weekly on Sundays at midnight

permissions:
  contents: read

jobs:
  government-api-build:
    name: Build, Test and Scan Government API
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up GO
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-  

      - name: Install dependencies
        run: |
          cd loan_validator_portal/backend && go mod tidy

      - name: Build project
        run: |
          cd loan_validator_portal/backend && go build -v ./...

      - name: Run tests
        run: |
          cd loan_validator_portal/backend && go test -v ./...

      - name: Scan code with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-path: './loan_validator_portal/backend'
          format: 'json'
          exit-code: '0'
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          output: 'government-trivy-report.json'

      - name: Upload Trivy scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: government-trivy-scan-report
          path: government-trivy-report.json

  loan-api-build:
    name: Build, Test and Scan Loan API
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up GO
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-  

      - name: Install dependencies
        run: |
          cd loan_validator_portal/backend && go mod tidy

      - name: Build project
        run: |
          cd loan_validator_portal/backend && go build -v ./...

      - name: Run tests
        run: |
          cd loan_validator_portal/backend && go test -v ./...

      - name: Scan code with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-path: './loan_validator_portal/backend'
          format: 'json'
          exit-code: '0'
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          output: 'loan-api-trivy-report.json'

      - name: Upload Trivy scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: loan-api-trivy-scan-report
          path: loan-api-trivy-report.json